import React from 'react';
import { GifList } from '../gifList/GifList';

export class Home extends React.Component {
  render() {
    return (
      <div>
        <GifList/>
      </div>
    );
  }
}
